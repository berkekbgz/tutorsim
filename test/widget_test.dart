// Milestone 1 has no widget tests yet — kept as a stub so `flutter test` passes.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
